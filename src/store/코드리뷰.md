### Content (추상 클래스)

public abstract class Content {    

    protected String title;   

     protected int price;

 }

**설명**

- Book / Movie / Music의 **공통 부모 클래스**

- `title`, `price`를 공통 필드로 관리

- `showInfo()`를 통해 **자식 클래스에서 오버라이딩 **

- `setPrice()`에서 **유효성 검사(음수 방지)** 수행

👉 공통 속성을 한 곳에서 관리하여 **중복 제거**

---

### Book / Movie / Music (하위 클래스)

public class Book extends Content {    

    private String author;    

    private boolean rented; 

}

**설명**

- `extends Content`로 공통 필드 상속

- 각 클래스는 **자신만의 필드**를 가짐
  
  - Book → author
  
  - Movie → runningTime
  
  - Music → singer

- `showInfo()`를 오버라이딩하여 **타입별 출력 구현**

---

### 3️⃣ Purchasable 인터페이스

public interface Purchasable {    

    void buy(); 

}

**설명**

- “구매 가능”이라는 **행위(기능)** 정의

- Book / Movie / Music 모두 구현

- 객체 타입에 상관없이 `buy()` 호출 가능

---

### 4️⃣ Rentable 인터페이스

public interface Rentable {    

    void rent();    

    void extendRent(); 

}

**설명**

- “대여 가능” 기능 정의

- Book / Movie만 구현

- Music은 구현하지 않아 **대여 불가**

👉 `instanceof`로 대여 가능 여부 판단

---

### 5️⃣ rented 필드 기반 상태 관리

if (rented) {     

    System.out.println("이미 대여 중입니다."); 

}

**설명**

- `boolean rented`로 **현재 상태 저장**

- 상태에 따라 다른 메시지 출력

- 객체 내부 상태에 따라 동작이 달라짐



---

### 6️⃣ Main 클래스 – 다형성 활용

`ArrayList<Content> contents = new ArrayList<>();`

**설명**

- 모든 콘텐츠를 `Content` 타입으로 관리

- 실제 객체는 Book / Movie / Music
